<?php require_once 'layout/header.phtml'; ?>
<div class="container row text-center">
    <?php
    $cont = 1;
     foreach($vuelos as $vuelo):?>
        <div class="card" style="width: 18rem;">
            <img src="<?= htmlspecialchars($vuelo->url_Imagen) ?>" class="card-img-top rounded mx-auto d-block" alt="...">
            <div class="card-body">
                <h5 class="card-title"><?= htmlspecialchars($vuelo->salida) ?> | <?= htmlspecialchars($vuelo->destino) ?> </h5>
                <p class="card-text">Precio por persona : $<?= $vuelo->precio ?></p>
                
                <div class="accordion" id="accordionExample">
                    <div class="accordion-item">
                        <h2 class="accordion-header">
                            <button class="accordion-button" type="button" data-bs-toggle="collapse" data-bs-target="#collapse<?= $cont?>" showaria-expanded="false" aria-controls="collapseOne" data-bs-targetidaria-controlsid>
                                Ver mas...
                            </button>
                        </h2>
                        <div id="collapse<?= $cont?>" class="accordion-collapse collapse" data-bs-parent="#accordionExample">
                            <div class="accordion-body">
                                <p>Dia: <?= htmlspecialchars($vuelo->fecha) ?></p>
                                <p>Avion: <?= htmlspecialchars($vuelo->avion) ?></p>
                                <p>Hora salida: <?=date('H:i', strtotime($vuelo->hs_salida)) ?> </p> 
                                <p>Hora llegada: <?= date('H:i', strtotime($vuelo->hs_llegada))?></p>

                                <a href="reservarVuelo/<?= $this->user->id?>/<?= $vuelo->id ?>" class="btn btn-primary" role="button">Reservar</a>
                            </div>
                        </div>
                    </div>

                </div>
            </div>
        </div>

    <?php
    $cont++;
    endforeach ?>
</div>
<?php require_once 'layout/footer.phtml'; ?>